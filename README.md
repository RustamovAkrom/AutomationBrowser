# Autonomous Browser AI Agent

Автономный AI-агент для работы с веб-браузером. Позволяет выполнять сложные многошаговые задачи без жёстко заданных скриптов, включая:

- Проверку и управление сессией пользователя
- Обход страниц и взаимодействие с элементами (кнопки, формы, ссылки)
- Сбор и обработку информации со страниц
- Автоматизацию задач: почта, вакансии, заказ еды и т.д.

---

## 🔹 Особенности

- **Автономное поведение**: агент принимает решения на основе видимых элементов, без хардкода URL или селекторов.
- **Сохранение сессий**: агент использует отдельный профиль Chrome (`autonomous_ai_profile`) для сохранения логинов между запусками.
- **Интерактивная авторизация**: если требуется вход, агент приостанавливает выполнение и ждёт ручного входа.
- **Оценка состояния авторизации**: подсчитывается **confidence score** (0.0–1.0), чтобы определить, нужно ли логиниться.
- **Расширяемость**: легко подключить новые "sub-agents" для специфичных задач.

---

## 🔹 Установка

1. Клонировать репозиторий:

```bash
git clone https://github.com/RustamovAkrom/AutomationBrowser.git
cd AutomationBrowser
````

2. Создать виртуальное окружение и установить зависимости:

```bash
python -m venv venv
source venv/Scripts/activate       # Windows
# или
source venv/bin/activate           # Linux/macOS

pip install -r requirements.txt
```

3. Установить браузер Chrome (если ещё не установлен).

4. Настроить `.env` файл (если нужно) и пути в `config/settings.py`:

```python
CHROME_PATH = r"C:\Program Files\Google\Chrome\Application\chrome.exe"
USER_DATA_DIR = r"%LOCALAPPDATA%\Google\Chrome\User Data"
PROFILE = "autonomous_ai_profile"
BROWSER_TIMEOUT = 30
PAGE_LOAD_WAIT = 5
```

---

## 🔹 Запуск

```bash
python main.py
```

После запуска в терминале:

1. Введите задачу (например: "Прочитай последние 5 писем и удали спам").
2. Агент автоматически откроет браузер и начнёт выполнение.
3. Если требуется авторизация, появится сообщение с инструкцией для ручного входа.
4. Введите `exit` для выхода из программы.

---

## 🔹 Структура проекта

```
AutomationBrowser/
├─ core/
│  ├─ agent.py           # Создание основного агента
│  ├─ browser.py         # Создание и настройка браузера
│  ├─ llm.py             # Создание AI модели (ChatBrowserUse)
│  ├─ skills.py          # Инструменты и действия агента
│  └─ auth_agent.py      # Под-агент для авторизации
├─ config/
│  └─ settings.py        # Настройки проекта и браузера
├─ prompts/
│  └─ system.txt         # Инструкции для AI
├─ main.py               # Точка входа проекта
├─ requirements.txt
└─ README.md
```

---

## 🔹 Как работает AuthAgent

* Проверяет **confidence score** авторизации на каждой целевой странице.
* Если score < 0.4 → нужно ручное логирование.
* Если score > 0.7 → авторизация подтверждена.
* Между этими значениями → состояние **UNCERTAIN**, агент может повторить проверку после действий пользователя.

---

## 🔹 Рекомендации

* Используйте отдельный профиль для агента, чтобы не сломать основной профиль Chrome.
* Для ускорения работы отключите ненужные расширения Chrome в профиле агента.
* Всегда проверяйте, что агент видит нужные элементы страницы (текст, кнопки), иначе confidence score может быть неправильным.

---

## 🔹 Примеры использования

### 1. Mail Handling

```
Задача: "Прочитай последние 10 писем и удали спам"
```

### 2. Food Ordering

```
Задача: "Закажи BBQ-бургер и картошку фри из того ресторана, откуда заказывал на прошлой неделе"
```

### 3. Job Application

```
Задача: "Найди подходящие вакансии AI-инженера на hh.ru и откликнись с сопроводительным письмом"
```

---

## 🔹 Лицензия

MIT License
